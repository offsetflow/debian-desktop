# Picom v13 configuration for dwm

# Rendering
backend = "glx";
vsync = true;
use-damage = true;
detect-client-opacity = true;
detect-transient = true;
detect-client-leader = true;
unredir-if-possible = true;

# Rounded corners
corner-radius = 10;
detect-rounded-corners = true;

# Soft shadows
shadow = true;
# Prevent a window shadow from bleeding into a neighboring monitor.
crop-shadow-to-monitor = true;
shadow-radius = 18;
shadow-offset-x = 0;
shadow-offset-y = 6;
shadow-opacity = 0.28;

# Fading
fading = true;
fade-delta = 4;
fade-in-step = 0.04;
fade-out-step = 0.04;

# Blur
blur-method = "dual_kawase";
blur-strength = 6;
blur-background = true;
blur-background-frame = false;
blur-background-fixed = true;

# Official Picom v13 animation presets.
animations = (
  {
    triggers = [ "open", "show" ];
    preset = "appear";
    duration = 0.18;
    scale = 0.96;
  },
  {
    triggers = [ "close", "hide" ];
    preset = "disappear";
    duration = 0.16;
    scale = 0.96;
  },
  {
    triggers = [ "geometry" ];
    preset = "geometry-change";
    duration = 0.14;
  }
);

# Window-specific behavior.
rules = (
  {
    match = "class_g = 'Polybar'";
    corner-radius = 0;
    shadow = false;
    blur-background = true;
  },
  {
    match = "class_g = 'dmenu'";
    corner-radius = 10;
    shadow = true;
    blur-background = true;
  },
  {
    match = "focused || group_focused";
    opacity = 1.0;
  },
  {
    match = "!focused && !group_focused && !override_redirect";
    opacity = 0.88;
  },
  {
    match = "fullscreen";
    opacity = 1.0;
    corner-radius = 0;
    shadow = false;
    blur-background = false;
  },
  {
    match = "border_width = 0";
    corner-radius = 0;
  },
  {
    match = "(window_type = 'dock' && class_g != 'Polybar') || window_type = 'desktop'";
    corner-radius = 0;
    shadow = false;
    blur-background = false;
  },
  {
    match = "_GTK_FRAME_EXTENTS@";
    shadow = false;
    blur-background = false;
  },
  {
    match = "class_g = 'mpv' || class_g = 'obs' || class_g = 'Gimp' || class_g = 'vncviewer'";
    opacity = 1.0;
  },
  {
    match = "class_g *= 'fcitx' || class_g *= 'wemeetapp' || name *= 'wechat'";
    corner-radius = 0;
    shadow = false;
    blur-background = false;
  },
  {
    match = "window_type = 'tooltip'";
    opacity = 0.55;
    shadow = false;
  }
);
